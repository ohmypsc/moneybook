# 우리 가계부 — ARCHITECTURE

> 최종 갱신: 2026-08-10 18:02 KST  
> 프로젝트: `moneybook`  
> 이 문서는 프로젝트의 장기 설계 원칙과 확정된 구조를 기록하는 **설계 기준 문서**다.  
> 과거 대화나 오래된 문서와 내용이 충돌하면 **이 문서와 최신 `AI_HANDOFF.md`를 우선**한다.

---

## 0. 문서 사용 원칙

이 프로젝트는 여러 대화창에 걸쳐 설계되었고 중간에 결정이 여러 번 변경되었다.

따라서 앞으로 AI나 개발자는 아래 우선순위를 따른다.

1. 실제 배포/실행 중인 최신 코드
2. `AI_HANDOFF.md`의 최신 진행 상태
3. 이 `ARCHITECTURE.md`의 확정 설계
4. 과거 대화 백업 파일
5. 초기 아이디어/검토안

과거 대화에 존재하더라도 후속 대화에서 폐기되거나 보류된 기능을 임의로 되살리지 않는다.

---

# 1. 프로젝트 목적

부부 2인이 사용하는 개인용 공유 가계부 웹앱/PWA를 만든다.

핵심 목표는 다음과 같다.

- 무료 또는 사실상 무료 범위에서 운영
- 모바일에서 앱처럼 편하게 사용
- 수입, 지출, 이체를 빠르게 입력
- 계좌, 카드, 현금, 지역화폐, 대출, 투자자산을 한 시스템에서 관리
- 부부 개인 지출과 공동 지출을 구분
- 카드 미결제액, 순자산, 투자 현황 등을 자동 계산
- 시트를 매일 직접 만지지 않고 모든 일상 입력/수정/확인을 프론트엔드에서 수행
- 데이터가 잘못되었을 때는 Google Sheets에서 직접 확인/복구할 수 있는 유지보수성 확보
- 복잡한 Google OAuth 흐름은 사용하지 않음
- GitHub의 코드를 중심으로 유지보수
- AI가 다른 대화에서도 구조를 빠르게 이해할 수 있도록 기능별 파일과 명확한 타입/계약을 유지

---

# 2. 전체 시스템 구조

최종 구조는 다음과 같다.

```text
사용자 브라우저 / PWA
        │
        │ HTTPS
        ▼
Cloudflare Worker
- 정적 프론트엔드 제공
- 로그인/세션
- API 게이트웨이
- 서버 Secret 보관
        │
        │ 서버 측 요청
        ▼
Google Apps Script v2.2.0
- 업무 규칙
- 입력 검증
- CRUD
- 집계
- 동시쓰기 잠금
- 로그/무결성
        │
        ▼
Google Sheets
- 원본 데이터
- 일부 계산 수식
- 수동 복구 가능한 저장소
```

GitHub의 `moneybook` 저장소가 코드의 기준 저장소이며 `main` 브랜치 push 시 Cloudflare가 자동 배포한다.

---

# 3. 역할 분담

## 3.1 Google Sheets

Google Sheets는 다음 역할을 담당한다.

- 원본 거래 데이터 저장
- 계좌/카테고리/보유종목 기준 데이터 저장
- 사람이 긴급 시 직접 열어볼 수 있는 복구 가능한 데이터베이스
- 일부 계좌 잔액 및 투자 평가 관련 시트 계산
- 시스템 로그 및 백엔드 테스트 결과 보관

사용자용 대시보드 UI를 Google Sheets에 별도로 만들지 않는다.

## 3.2 Apps Script

Apps Script는 핵심 백엔드다.

담당 범위:

- 거래 CRUD
- 계좌 CRUD
- 카테고리 CRUD
- 구성원 CRUD
- 보유종목 CRUD
- 투자 거래 처리
- 수입/지출/이체 검증
- 체크카드/신용카드/지역화폐/대출 규칙
- 부분 환불 및 취소
- 투자 매수/매도
- 평균매입가
- 실현손익
- 예수금
- 국내/해외 투자 처리
- 대시보드용 집계 JSON
- ID 기반 연결
- soft delete / restore
- request ID 기반 중복 요청 방지
- ScriptLock 기반 동시 쓰기
- 행 자동 확장
- 시스템 로그
- 무결성 검사

Apps Script 백엔드 최종 기준 버전은 **v2.2.0**이다.

특별한 문제나 새로운 요구가 없는 한 v2.2.0의 기존 동작을 임의로 바꾸지 않는다.

## 3.3 Cloudflare Worker

Cloudflare는 다음 역할만 담당한다.

- 프론트엔드 정적 파일 배포
- 사용자 로그인
- 장기 세션 쿠키
- 브라우저 대신 Apps Script 호출
- Apps Script URL 및 API Secret 은닉
- 향후 `/api/*` 프록시

Cloudflare 자체를 별도의 데이터베이스로 사용하지 않는다.

## 3.4 프론트엔드

프론트엔드는 사용자에게 보이는 모든 화면을 담당한다.

- 홈
- 거래 입력
- 거래 내역
- 계좌/자산
- 카드
- 투자
- 설정
- 차트
- 검색/필터/정렬
- 카테고리 및 계좌 등의 관리 UI

중요 계산 규칙을 프론트엔드에 중복 구현하지 않는다.
프론트엔드는 백엔드의 계산 결과를 표시하는 쪽을 우선한다.

---

# 4. 백엔드 완료 기준

Apps Script v2.2.0 통합 테스트의 최종 결과:

```text
상태: SUCCESS
PASS: 25
FAIL: 0
최종 무결성 오류: 0
```

검증된 기능:

- 일반 거래
- 수입
- 지출
- 이체
- 체크카드
- 신용카드
- 카드 선결제
- 지역화폐
- 대출
- 부분 환불/취소
- 구성원 CRUD
- 계좌 CRUD
- 카테고리 CRUD
- 보유종목 CRUD
- ID 연결 안정성
- 주식 매수
- 추가매수
- 매도
- 평균매입가
- 실현손익
- 예수금
- 해외주식
- 투자 CRUD
- 삭제/복원
- 중복 요청 방지
- 가계부 시작일
- 테스트 데이터 정리
- 최종 무결성

Apps Script 테스트는 실행시간 제한 때문에 최종적으로 25개 세부 단계로 나누어 시간 기반 트리거 방식으로 검증했다.

---

# 5. Google Sheets 데이터 구조

## 5.1 핵심 탭

핵심 데이터 탭:

- `카테고리`
- `계좌`
- `가계부`
- `보유종목`

백엔드 보조 탭:

- `시스템_로그`
- `백엔드테스트`
- v2.2가 투자 처리 및 테스트를 위해 사용하는 기타 보조 시트가 있을 수 있음

> 기술 컬럼이나 보조 시트의 정확한 이름/구조는 **최신 Code.gs를 최종 source of truth로 본다.**
> 문서를 기준으로 Code.gs 구조를 억지로 변경하지 않는다.

스프레드시트의 사용자용 `대시보드` 탭은 최종 UI 용도로 사용하지 않는다.
필요 시 계산/검증용으로만 둘 수 있지만 사용자 화면은 프론트엔드에서 만든다.

---

## 5.2 카테고리

핵심 개념:

- 유형은 `수입 / 지출 / 이체`
- 대분류
- 사용여부
- 구성원/지출대상 기준 데이터
- 각 항목에는 백엔드가 관리하는 영구 ID가 존재

카테고리는 삭제 시 행을 물리적으로 없애는 방식보다 soft delete/비활성화 원칙을 우선한다.
과거 거래가 깨지지 않아야 한다.

프론트엔드에서 카테고리 추가/수정/삭제/복원을 할 수 있도록 한다.

---

## 5.3 계좌 탭의 확정 가시 구조

현재 권장 구조:

| 열 | 항목 |
|---|---|
| A | 계좌명 |
| B | 계좌유형 |
| C | 세부구분 |
| D | 명의자 |
| E | 시작잔액 |
| F | 청구마감일 |
| G | 결제일 |
| H | 사용시작연도 |
| I | 사용종료연도 |
| J | 사용여부 |
| K | 표시명 |
| L | 현재잔액자동 |
| M | 잔액계산방식 |
| N | 현재잔액최종 |
| O | 결제계좌 |
| P | 자산귀속 |

기술 컬럼은 오른쪽에 백엔드가 추가/관리한다.

중요 용어:

- `소유자`보다 **명의자**를 사용자용 용어로 사용
- `초기금액`보다 **시작잔액**을 사용
- 계좌별 초기기준일 대신 전역 **가계부 시작일** 개념을 사용

---

# 6. 가계부 시작일과 시작잔액

전역 가계부 시작일을 기준으로 관리한다.

예:

```text
가계부 시작일: 2026-08-01
시작잔액: 2026-08-01 거래 발생 전 잔액
가계부 거래: 시작일 이후 발생 건부터 기록
```

일반 계좌의 기본 계산 원칙:

```text
현재잔액
= 시작잔액
- 출금으로 기록된 금액
+ 입금으로 기록된 금액
```

이 규칙으로 은행, 현금, 카드 부채, 대출 등이 동일한 흐름으로 움직인다.

---

# 7. 가계부 거래 모델

사용자에게는 내부 DB 구조가 아니라 자연스러운 용어를 보여준다.

## 수입

사용자 입력:

- 날짜
- 금액
- 카테고리
- **입금수단**
- 메모

규칙:

- 입금수단 필수
- 출금수단은 사용하지 않음

## 지출

사용자 입력:

- 날짜
- 금액
- 카테고리
- **결제수단**
- **지출대상**
- 메모

규칙:

- 결제수단 필수
- 지출대상 필수
- 입금수단은 사용하지 않음

## 이체

사용자 입력:

- 날짜
- 금액
- 카테고리
- **보내는 수단**
- **받는 수단**
- 메모

규칙:

- 보내는 수단 필수
- 받는 수단 필수
- 두 수단은 같을 수 없음
- 지출대상은 사용하지 않음

---

# 8. 사용자 용어 규칙

최종 프론트엔드에서는 다음 표현을 사용한다.

- `지출귀속` → **지출대상**
- `소유자` → 계좌에서는 **명의자**
- 지출 화면의 `출금계좌` → **결제수단**
- 수입 화면의 `입금계좌` → **입금수단**
- 이체 화면 → **보내는 수단 / 받는 수단**

기존 시트 헤더나 백엔드 alias는 하위 호환을 위해 과거 용어를 일부 인식할 수 있으나,
새 UI에서는 최신 용어만 사용한다.

---

# 9. 지출대상

명의자와 지출대상은 다른 개념이다.

예:

```text
명의자: 미영
결제수단: 미영 명의 카드
지출대상: 공동
```

지출대상은 사용자가 매 지출마다 수동 선택한다.

프론트엔드는 지출대상을 하드코딩하지 않고
`bootstrap`에서 내려오는 `spendingTargets`를 사용한다.

구성원이 바뀌어도 코드를 수정하지 않아야 한다.

---

# 10. 카드 및 결제수단 규칙

## 10.1 신용카드

신용카드는 **부채 계좌**다.

카드 사용:

```text
지출
결제수단 = 신용카드
→ 카드 잔액이 음수 방향으로 증가
```

결제:

```text
이체
보내는 수단 = 은행계좌
받는 수단 = 신용카드
→ 은행 잔액 감소
→ 카드 부채 감소
```

## 10.2 체크카드

사용자는 화면에서 체크카드를 선택한다.

백엔드 저장 개념:

```text
payment_method_id = 체크카드 ID
from_account_id = 체크카드에 연결된 실제 통장 ID
```

따라서:

- 결제수단 정보는 체크카드로 보존
- 실제 돈은 연결 통장에서 즉시 차감
- 체크카드 자체에는 신용카드처럼 부채를 만들지 않음

이 규칙은 프론트엔드에서 재구현하지 않고 백엔드에 맡긴다.

## 10.3 지역화폐/선불자산

지역화폐는 독립 자산으로 관리한다.

- 충전: 자산 간 이체
- 사용: 지역화폐 자산에서 지출
- 체크카드처럼 반드시 연결계좌를 요구하지 않음

## 10.4 카드 선결제와 정기결제

카드 부채 잔액은 기본 잔액 공식으로 정확히 반영한다.

선결제:

```text
은행 → 카드 이체
```

정기결제:

```text
은행 → 카드 이체
```

청구 회차 분석이 필요할 경우 대상청구월을 사용한다.

카드 관련 월 값은 문자열보다 날짜로 저장하고 화면 형식만 `yyyy-mm`을 사용한다.

---

# 11. 대출 규칙

대출은 부채 계좌다.

가계부 시작 시점의 **실제 남은 원금**을 음수 시작잔액으로 입력한다.

예:

```text
남은 주택담보대출 원금 150,000,000원
→ 시작잔액 = -150000000
```

원금 상환:

```text
이체
보내는 수단 = 은행계좌
받는 수단 = 대출계좌
```

대출계좌가 음수이므로 입금될수록 0에 가까워져 부채가 감소한다.

최종 결정상 대출 상환은 **원금과 이자를 분리**한다.

- 원금 → 이체
- 이자 → 지출 `이자비용`

초기 대화의 “원금/이자를 한 번에 처리” 아이디어는 폐기된 결정으로 본다.

---

# 12. 보유종목 / 투자

투자자산은 은행잔액과 달리 시장가격 변동이 있으므로 별도 계산한다.

보유종목 기본 정보:

- 보유계좌
- 종목코드
- 종목명
- 시장
- 수량
- 평균매입가
- 시세조회방식
- 수동입력가
- 현재가
- 환율
- 평가금액(원화)
- 매입금액(원화)
- 수익률
- 명의자/소유자 자동
- 마지막 업데이트일
- 경과일수

국내/해외 투자 평가금액은 원화 기준으로 통일한다.

GOOGLEFINANCE로 조회할 수 없는 자산은 수동 시세를 지원한다.

백엔드 v2.2는 다음을 검증 완료했다.

- 매수
- 추가매수
- 매도
- 평균매입가
- 실현손익
- 해외주식
- 예수금
- 투자 CRUD

---

# 13. 투자계좌 예수금

투자계좌의 총 자산은 다음 개념을 사용한다.

```text
투자계좌 총자산
= 예수금
+ 보유종목 평가금액
```

실제 투자계좌의 최초 예수금 기준값은 아직 사용자가 프론트엔드에서 입력하도록 만들 예정이다.

중요:

보유종목 평가금액은 이미 계산되므로 최초 예수금 입력 시 주식 평가액까지 중복 입력하면 안 된다.

최종 UX 목표:

```text
연금저축
현재 예수금을 입력해주세요.

[ 327,500 ] 원
[ 저장 ]
```

---

# 14. ID 및 데이터 안정성 원칙

프론트엔드는 화면에 사람이 읽는 이름을 표시하더라도 저장/수정의 기준은 영구 ID를 사용한다.

대표 ID:

- `category_id`
- `account_id`
- `transaction_id`
- `holding_id`
- `payment_method_id`
- `from_account_id`
- `to_account_id`
- `request_id`

행 번호를 영구 식별자로 사용하지 않는다.

예:

```text
잘못된 방식:
17번째 행 수정

올바른 방식:
transaction_id = TX_xxxxx 수정
```

---

# 15. 삭제 원칙

기본적으로 실제 행 삭제보다 soft delete를 사용한다.

이유:

- 과거 연결 보존
- 복원 가능
- 잘못된 삭제 대응
- 감사/로그 추적

백엔드는 delete/restore를 지원한다.

---

# 16. 행 자동 확장

프론트엔드 운영 후 사용자가 1000행 제한을 직접 관리하지 않는다.

Apps Script가 필요 시 행을 자동으로 추가하고:

- 서식
- 데이터 유효성
- 필요한 개별 수식

을 복사/적용한다.

사용자는 시트의 드롭다운 범위를 주기적으로 늘릴 필요가 없어야 한다.

---

# 17. 대시보드 전략

Google Sheets의 사용자용 대시보드는 만들지 않는다.

이유:

- 시트 대시보드와 웹 대시보드를 이중 제작하지 않기 위해
- 모든 실제 사용을 프론트엔드로 통일하기 위해

Apps Script가 집계 데이터를 JSON으로 계산하고 프론트엔드가 표시한다.

주요 프론트엔드 집계 후보:

- 총자산
- 총부채
- 순자산
- 투자자산
- 현금성자산
- 이번 달 수입
- 이번 달 지출
- 순현금흐름
- 카테고리별 지출
- 지출대상별 지출
- 카드 결제예정
- 투자 수익률
- 최근 거래

---

# 18. 보안 아키텍처

## 18.1 Apps Script 배포

Apps Script 웹앱은 Cloudflare Worker가 서버 측에서 호출할 수 있도록
Google 로그인 없이 접근 가능한 웹앱 배포로 설정한다.

보안은 URL 비공개만으로 의존하지 않는다.

## 18.2 Cloudflare Secrets

현재 사용하는 Secret 이름:

```text
APPS_SCRIPT_URL
LEDGER_API_SECRET
LOGIN_USERS
SESSION_SECRET
```

중요:

- 실제 Secret 값은 GitHub에 저장하지 않음
- 실제 Secret 값은 문서에 기록하지 않음
- 사용자에게 Secret 값을 대화에 붙여넣게 요구하지 않음

`LOGIN_USERS`는 구성원 이름과 로그인 비밀번호 매핑을 JSON으로 보관한다.

한글 구성원 이름 사용 가능.

## 18.3 Cloudflare → Apps Script 인증

브라우저는 `LEDGER_API_SECRET`을 알지 못한다.

흐름:

```text
브라우저
→ /api/...
→ Cloudflare Worker
→ LEDGER_API_SECRET 자동 첨부
→ Apps Script
→ 검증 후 처리
```

`health` 같은 연결 확인 엔드포인트를 제외한 실제 데이터 API는 Secret 검증을 거치는 구조를 유지한다.

---

# 19. 로그인 / 세션

로그인은 Cloudflare Worker가 담당한다.

세션 쿠키 원칙:

- HMAC 서명
- HttpOnly
- Secure
- SameSite=Strict
- 쿠키 이름: `__Host-moneybook_session`

최신 결정은 **400일 sliding session**이다.

즉:

- 최초 로그인 후 최대 400일 세션 발급
- 앱이 `/api/auth/session`을 통해 정상 세션을 확인할 때 새 400일 세션을 발급
- 400일 안에 앱을 계속 사용하는 한 사실상 로그인 상태가 계속 연장됨
- 브라우저 쿠키 삭제, SESSION_SECRET 변경, 400일 이상 미사용 시 재로그인 필요

최종 사용자 화면에는 “400일”, “세션”, “보안 연결” 같은 개발용 문구를 보여주지 않는다.

---

# 20. Cloudflare / GitHub 배포 구조

현재 공개 주소:

```text
https://moneybook.mysc0711.workers.dev
```

현재 저장소 배포 원칙:

```text
moneybook/
├─ public/
│  └─ index.html
├─ src/
│  └─ index.js       # 현재 Worker 코드 위치
└─ wrangler.jsonc
```

`wrangler.jsonc`는 `public/` 안이 아니라 **저장소 루트**에 둔다.

현재 정적 자산:

```text
assets.directory = ./public
```

API 경로는 Worker가 처리하고 일반 화면은 정적 자산으로 제공한다.

향후 프론트엔드 프레임워크를 도입하면 디렉터리 구조는 변경 가능하지만,
브라우저 → Cloudflare → Apps Script 구조는 유지한다.

---

# 21. 연결 검증 완료 상태

Cloudflare Worker 자체:

```json
{
  "success": true,
  "message": "Cloudflare Worker is running."
}
```

Apps Script health:

```json
{
  "success": true,
  "apiVersion": "2.2.0",
  "service": "부부가계부",
  "status": "ok"
}
```

Cloudflare Secret을 통한 백엔드 인증 테스트:

```text
success: true
backendAuthenticated: true
apiVersion: 2.2.0
```

마지막 관측 데이터 개수:

```text
accounts: 18
categories: 23
members: 2
spendingTargets: 3
```

이 개수는 실제 데이터 변경에 따라 달라질 수 있으므로 설계 상수로 사용하지 않는다.

---

# 22. 프론트엔드 UX 원칙

최종 프론트엔드는 **가계부 역할에만 충실**해야 한다.

사용자에게 노출하지 않을 것:

- 보안 연결 확인 버튼
- API 버전
- 백엔드 계좌 개수
- 카테고리 개수
- Worker ping
- session day 정보
- Apps Script URL 입력창

앱 시작 시 내부적으로:

```text
세션 확인
→ bootstrap 로드
→ 정상이라면 앱 표시
→ 실패한 경우에만 사용자에게 오류 표시
```

개발 진단 기능은 필요하면 숨겨진 개발 모드/로그에서 확인한다.

---

# 23. 프론트엔드 기술 선택 원칙

**프론트엔드 프레임워크는 아직 최종 확정하지 않았다.**

React는 사용자가 예시로 언급했을 뿐이다.

후보:

- React + TypeScript + Vite
- Vue 3 + TypeScript + Vite
- 그 외 유지보수성이 충분한 컴포넌트 기반 구조

선택 기준은 “사용자가 직접 코딩하기 쉬운가”보다 다음을 우선한다.

1. 장기 유지보수
2. 기능 확장 용이성
3. 컴포넌트 분리 명확성
4. 전역 스타일/디자인 토큰 관리
5. API 데이터 타입 명확성
6. AI가 새 대화에서 코드를 쉽게 이해할 수 있는 구조
7. 생태계와 문서가 충분함
8. Cloudflare 배포가 단순함

프레임워크를 확정하기 전까지 문서나 코드에서 React/Vue를 확정 사실처럼 기록하지 않는다.

---

# 24. 프론트엔드 코드 구성 원칙

어떤 프레임워크를 선택하더라도 다음 원칙은 고정한다.

## 기능 분리

예상 구조 개념:

```text
frontend/
├─ api/
├─ types/
├─ components/
│  ├─ common/
│  └─ layout/
├─ pages/ 또는 views/
├─ features/
│  ├─ transactions/
│  ├─ accounts/
│  └─ investments/
└─ styles/
```

## 전역 디자인 토큰

색상, 간격, border radius, shadow 등은 한 파일에서 관리한다.

예:

```css
:root {
  --color-primary: ...;
  --color-background: ...;
  --color-surface: ...;
  --color-text: ...;
  --color-muted: ...;

  --space-1: ...;
  --radius-md: ...;
}
```

화면마다 임의 색상 값을 반복해서 하드코딩하지 않는다.

## 공통 컴포넌트

예:

- Button
- Card
- Modal
- Select
- Input
- Money
- EmptyState
- Loading
- Toast
- BottomNavigation

공통 스타일을 화면별로 복사하지 않는다.

## API 전담 레이어

화면 컴포넌트에서 직접 `fetch()` 코드를 반복하지 않는다.

예:

```text
api/client
api/ledger
```

같은 계층에서 요청/응답/오류/401 처리를 통일한다.

---

# 25. 최종 화면 방향

권장 하단 메뉴:

```text
홈 | 내역 | + | 자산 | 투자
```

중앙 `+`:

```text
수입 | 지출 | 이체
```

주요 화면:

## 홈

- 이번 달 수입
- 이번 달 지출
- 잔액/순현금흐름
- 순자산
- 최근 거래
- 필요한 카드/자산 요약

## 내역

- 거래 목록
- 월 이동
- 검색
- 카테고리 필터
- 지출대상 필터
- 수정
- 삭제/복원

## 거래 입력

유형을 선택하면 필요한 필드만 보여준다.

## 자산

- 계좌
- 예적금
- 현금
- 지역화폐
- 카드 부채
- 대출
- 순자산

## 투자

- 계좌별 투자자산
- 보유종목
- 국내/해외 원화 합산
- 예수금
- 매수/매도
- 평가금액
- 손익
- 수동시세 갱신 경고

---

# 26. PWA

최종적으로 PWA를 적용한다.

목표:

- 휴대폰 홈 화면 설치
- 앱 아이콘
- standalone 표시
- 모바일 최적화

PWA는 핵심 가계부 기능이 안정된 뒤 적용한다.

---

# 27. 현재 보류 기능 / 향후 백로그

## 연말정산 미터기

**지금은 제외. 내년부터 추가 예정.**

따라서 과거 대화에서 제안된 연말정산용 가계부 K~Q 열은 현재 핵심 구조에 강제로 추가하지 않는다.

향후 검토 내용:

- 개인별 예상 총급여
- 총급여 25% 기준
- 신용카드/체크카드/현금영수증
- 전통시장/대중교통/문화체육
- 공제 제외 거래
- 예상 소득공제

## 단기 유동성 경고

향후:

```text
현금성자산
vs
곧 확정되는 카드/고정비
```

비교 기능 검토.

## 비정기/대형 지출

향후 별도 계획 기능 검토.

## 투자 자동 연동

현재는 Google Sheets/Apps Script 기반 구조를 유지한다.
증권사 API 자동연동은 후속 고도화 기능으로 본다.

---

# 28. 금지/주의 사항

- Apps Script URL을 프론트엔드 코드에 직접 넣지 않음
- `LEDGER_API_SECRET`을 프론트엔드에 넣지 않음
- Cloudflare Secret 값을 문서/커밋에 기록하지 않음
- Google Sheets 행 번호를 영구 ID로 사용하지 않음
- 체크카드 로직을 프론트에서 별도로 계산하지 않음
- 사용자용 대시보드를 Sheets에서 다시 만들지 않음
- 지출대상을 카드 명의자로 자동 결정하지 않음
- `spendingTargets`나 구성원 이름을 프론트에 하드코딩하지 않음
- 연말정산 기능을 현재 범위에 임의로 끼워 넣지 않음
- 프론트엔드 프레임워크를 사용자의 최종 확인 없이 확정하지 않음
- 개발용 연결 테스트 UI를 최종 제품 화면에 남기지 않음

---

# 29. AI 작업 원칙

AI가 이 프로젝트를 이어받을 때:

1. 먼저 `ARCHITECTURE.md`를 읽는다.
2. 그다음 `AI_HANDOFF.md`를 읽는다.
3. 코드 수정 전 현재 파일 구조를 확인한다.
4. 과거 백업 대화보다 이 두 문서를 우선한다.
5. 확정되지 않은 사항을 임의로 확정하지 않는다.
6. 기존 백엔드 v2.2 동작을 불필요하게 재설계하지 않는다.
7. 변경 시 관련 문서를 함께 갱신한다.
8. 사용자에게 코드를 줄 때 가능하면 **파일 단위 완성본**으로 제공한다.
9. Secret 값은 요청하거나 출력하지 않는다.
10. 실제 배포 여부가 확인되지 않은 변경은 `완료`라고 기록하지 않는다.

