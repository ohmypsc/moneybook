# 2026-09-14 업데이트

## 변경 내용

1. 자산 화면 투자 요약의 명의자 필터 수정
   - 투자자산 `합계`뿐 아니라 `예수금`, `실현손익`도 선택한 명의자 기준으로 계산합니다.
   - `전체`를 선택하면 기존처럼 모든 명의자의 합계를 표시합니다.

2. 지출 입력의 결제수단 선택 개선
   - 기본 목록에는 로그인한 사용자 명의와 `공동` 결제수단을 우선 표시합니다.
   - 다른 명의의 결제수단은 `다른 명의 결제수단 N개 · 더보기`로 접어 둡니다.
   - 더보기를 누르면 다른 명의의 결제수단을 선택할 수 있고, 다시 접을 수 있습니다.
   - 입력 설정에서 숨겨진 계좌라도 다른 명의의 `신용카드`/`체크카드`는 더보기 안에서 선택할 수 있습니다.
   - 다른 명의 결제수단이 이미 선택되어 있으면 선택창을 다시 열 때 해당 영역을 자동으로 펼칩니다.

## 수정 파일

- `src/pages/AssetsPage/AssetsPage.tsx`
- `src/pages/InputPage/InputPage.tsx`
- `src/pages/InputPage/InputPage.module.css`
